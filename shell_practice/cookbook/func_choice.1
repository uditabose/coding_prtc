#!/bin/bash

function choice {
	CHOICE=''
	#echo "$*"
	local prompt="$*"
	local answer
	
	read -p "$prompt" answer
	
	case "$answer" in
	    [yY1] ) CHOICE='y';;
	    [nN0] ) CHOICE='n';;
	    *     ) CHOICE="$answer";;
	esac

	echo $CHOICE
}

choice $@
